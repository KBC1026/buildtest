
# 프로젝트 청사진: 제비뽑기 웹 애플리케이션

## 개요

이 프로젝트는 사용자가 전체 제비 수와 당첨 수를 설정하여 제비뽑기를 할 수 있는 간단한 인터랙티브 웹 애플리케이션입니다. HTML, CSS, JavaScript로 구성된 단일 페이지 애플리케이션입니다.

## 구현된 기능 및 스타일

*   **HTML:** 제비뽑기 설정 (전체 수, 당첨 수, 표시 이름)을 위한 컨트롤과 제비가 표시될 영역, 결과 메시지 영역으로 구성됩니다.
*   **CSS:** 모던하고 깔끔한 디자인을 적용했습니다.
    *   반응형 레이아웃으로 모바일 환경에서도 사용이 편리합니다.
    *   `light` 컬러 스킴을 기반으로 하며, `--primary`, `--win`, `--lose` 등 CSS 변수를 사용하여 색상을 관리합니다.
    *   입력 컨트롤, 버튼, 제비 영역에 명확한 스타일과 호버 효과를 적용하여 사용자 경험을 향상시킵니다.
*   **JavaScript:**
    *   사용자가 설정한 값에 따라 제비를 동적으로 생성하고 섞습니다.
    *   각 제비를 클릭하면 '당첨' 또는 '꽝' 결과를 표시합니다.
    *   '섞기' 버튼으로 제비를 다시 생성하고, '초기화' 버튼으로 모든 설정을 기본값으로 되돌립니다.
    *   뽑기 결과에 따라 적절한 메시지를 사용자에게 보여줍니다.

## 현재 계획: Cloudflare Pages 배포 자동화 설정

GitHub와의 연동을 통해 CI/CD(지속적인 통합/지속적인 배포) 파이프라인을 구축하여, GitHub 저장소에 코드가 푸시될 때마다 자동으로 Cloudflare Pages에 배포되도록 설정합니다.

### 설정 단계

1.  **Cloudflare 대시보드 접속:** Cloudflare Pages 대시보드에서 '계정에서 사이트 만들기'를 선택합니다.
2.  **GitHub에 연결:** 'GitHub에 연결' 버튼을 클릭하여 GitHub 계정 인증 및 권한 부여 팝업창을 진행합니다.
3.  **저장소 선택:** 배포할 프로젝트의 GitHub 저장소를 선택합니다.
4.  **빌드 설정 구성:**
    *   **프로덕션 브랜치:** `main` 또는 `master`를 선택합니다.
    *   **빌드 명령어:** **비워 둡니다.** (이 프로젝트는 별도의 빌드 과정이 필요 없는 정적 파일로만 구성되어 있습니다.)
    *   **빌드 출력 디렉터리:** `/` 또는 `.` 를 입력하여 **프로젝트 루트 디렉토리**를 지정합니다.
5.  **저장 및 배포:** 설정을 저장하고 첫 배포를 시작합니다. 배포가 완료되면 고유한 `.pages.dev` URL이 생성됩니다.

